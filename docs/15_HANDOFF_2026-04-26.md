# 15 — Handoff: 26 April 2026

> Implementer mode this session (planner/implementer split paused at user request: "i want you to implement for revision this time").
> Branch: `main`. Migration head: `f5398a2b1eb5`.

---

## What got done today

### 1. Stage 3 revision requirements — all 6 implemented

| # | Item | Where |
|---|---|---|
| 1 | Meeting scheduling 2 months ahead | `templates/admin/meetings.html` (date input `min/max`) |
| 2 | Meeting types `regular | closed | launchpad` | `Meeting.meeting_type` + CHECK constraint, `templates/admin/meetings.html` dropdown |
| 3 | Dual feature presenter (0 / 1 / 2 speakers) | `meta.feature_presenter_id`, `meta.feature_presenter_2_id`; `templates/presenter/slide_feature_presentation.html` |
| 4 | Weekly notes / dokumentasi | `meetings.weekly_notes TEXT` column |
| 5 | Edu + Core Value host rotation | `host_rotation_state` table; `app/host_rotation.py` (round-robin, 8-week cooldown) |
| 6 | Role-gated meeting editor | `/admin/meetings/{id}/edit` + `PATCH /api/admin/meetings/{id}`; `auth.can_edit_meeting_field()` |

Migration applied: `alembic/versions/f5398a2b1eb5_meeting_revisions.py` (down_revision: `e9e20ec705ef`).

### 2. Presenter wiring bug fixed
`/presenter/{id}` was wired to a stub in `app/main.py` that didn't pass `slides`. Removed the inline route; now wired directly to `routes_presenter.presenter_view`.

### 3. Presenter UI redesign — sample-driven
- New shell `templates/presenter.html` ported from `docs/wm-preview.html`: top-nav, tab strip, 16:9 white slide-frame, fadeIn, fullscreen, 5s polling.
- 10 slide templates redesigned to white-card BNI aesthetic (red clip-path header, divider-green, swoosh, BNI-corner logo):
  - `wm_title`, `open_networking`, `welcome_video`, `etiquettes`, `disclaimer`, `global_stats`
  - `member_card`, `feature_presentation`, `category_header`, `closing`
- Remaining 17 legacy-dark slides are now **filtered out** of the deck (per user request) until they get HTML mockups.

### 4. Demo meeting seeded
- Wed **2026-04-29** "Visitors Day" — meeting_id `7afcedda-7da1-4db0-9698-33d1033a9e7e`
- Dual FP: Bobby Hartanto + Stella Clarissa
- Core Values host: Henny Setiadi · Education host: Kevin Christian
- All 51 active members marked Present.
- URL: https://bnimahardika.qd.je/presenter/7afcedda-7da1-4db0-9698-33d1033a9e7e

---

## Workflow change agreed today

**Vanilla HTML+Jinja now → JS layer later.** User will:
1. Send an HTML mockup for a given slide.
2. Claude ports it to `templates/presenter/slide_<name>.html`, wired to existing `slide.data` and `chapter`/`meeting` context.
3. User adds animations / Alpine / interactive JS on top of the static port.

When richer interactivity is wanted, two clean upgrade paths (no rewrite needed):
- **HTMX** drop-in for partial swaps.
- **SPA** (Vue/Alpine/React) consuming a JSON deck endpoint we'd add (`routes_presenter.py` already builds a structured `slides` list).

---

## Tomorrow's TODO (priority order)

### Hot path — needed before Wed 29 Apr meeting
1. **Receive HTML mockups** from user for the high-impact remaining slides:
   - `slide_lt_roster` (Leadership Team — high signal, 51 chapter)
   - `slide_top3` (Top 3 RGI / TYFCB / Visitors)
   - `slide_vp_report` (chapter health stats)
   - `slide_visitor_intro` (Visitors Day theme — critical for 2026-04-29)
   - `slide_thank_visitors` (closing visitor moment)
2. Port each to a white-card Jinja partial; add to the `redesigned` allow-list in `templates/presenter.html`.
3. Re-run end-to-end: navigate the full deck against the seeded meeting, screenshot for record.

### Warm path — second priority
4. Mockups + ports for: `slide_core_values`, `slide_education_moment`, `slide_mission_vision`, `slide_bni_overview`, `slide_code_of_ethics`, `slide_etiquettes` enhancements, `slide_gold_members`, `slide_green_members`, `slide_new_member`.
5. `slide_announcements` — file-upload poster slide-type from spec needs admin upload UI; not a slide redesign alone.
6. `slide_tyfcb_lifetime`, `slide_did_we_miss`, `slide_still_looking`, `slide_photo_session`, `slide_open_networking` (already done — confirm).

### Cold path — after the meeting
7. **Live admin sync** — admin clicks Next, every viewer screen follows. Polling endpoint exists; hook the Edit-Meeting / admin presenter to `POST /api/presenter/{id}/state` so it actually advances state, not just local nav.
8. **Auto-hide absent members** — `routes_presenter.py:get_present_members` already filters; verify `member_card` slides show NEXT presenter correctly when someone is absent.
9. **PPTX → PDF async pipeline** (Stage 3 spec Q5). Spec'd, not built.
10. **Member-card editor at `/member/card`** — self-service so each member maintains their own card. Backend `routes_cards.py` exists; UX/UI pass.
11. **Rotation auto-suggest UI** — `host_rotation_state` is wired in create_meeting_api; expose suggestion in the meeting-edit page (button "Suggest next host") via `GET /api/admin/host-suggest/{rotation_type}`.
12. **Member-roles cleanup** — DB shows duplicate `member` rows per member (103 rows for 51 members). Investigate `member_roles` seeding and dedupe.

### Hygiene
- Update `docs/13_WM_PRESENTER_SPEC.md` — mark revision items DONE, link to this handoff.
- Commit pending changes (today's session has uncommitted work in `docs/`, `templates/`, `app/`, new migration).

---

## Files touched today

```
NEW
  alembic/versions/f5398a2b1eb5_meeting_revisions.py
  app/host_rotation.py
  templates/admin/meeting_edit.html
  docs/15_HANDOFF_2026-04-26.md (this file)

EDITED
  app/auth.py                                   (+role helpers, can_edit_meeting_field)
  app/main.py                                   (wire new routes; remove buggy inline presenter route)
  app/models.py                                 (Meeting.weekly_notes, CHECK, HostRotationState)
  app/routes_meeting.py                         (rewrite — types, dual FP, role gating, update API)
  app/routes_presenter.py                       (dual FP rendering, closed-meeting skip)
  templates/admin/meetings.html                 (date range, type, FP2, weekly notes)
  templates/presenter.html                      (new shell — top-nav, tabs, 16:9, polling)
  templates/presenter/slide_open_networking.html
  templates/presenter/slide_welcome_video.html
  templates/presenter/slide_etiquettes.html
  templates/presenter/slide_disclaimer.html
  templates/presenter/slide_global_stats.html
  templates/presenter/slide_wm_title.html
  templates/presenter/slide_member_card.html
  templates/presenter/slide_feature_presentation.html
  templates/presenter/slide_category_header.html
  templates/presenter/slide_closing.html
  docs/14_HANDOFF_2026-04-22.md                 (mark revisions implemented)
```

---

## Quick references for tomorrow's session

- Demo meeting URL: `/presenter/7afcedda-7da1-4db0-9698-33d1033a9e7e`
- Seed meeting via SQL: see `/tmp/seed_meeting.sql` (uses `uuid_generate_v4()` — `pgcrypto` is NOT installed, only `uuid-ossp`).
- App container: `mahardika-app` (uvicorn) · DB: `mahardika-postgres` (user `mahardika` / db `mahardika_db`).
- Run migrations: `docker exec mahardika-app alembic upgrade head`.
- Restart app: `docker restart mahardika-app` (Jinja templates do NOT auto-reload — restart after editing).
- Auth: cookie-based JWT; `lucky@mierakigai.com` is the only `admin` role.
